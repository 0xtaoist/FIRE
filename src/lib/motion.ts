"use client";

import {
  div,
  p,
  h1,
  h2,
  span,
  button,
  section,
} from "framer-motion/client";

export { AnimatePresence } from "framer-motion";

export const motion = { div, p, h1, h2, span, button, section };
